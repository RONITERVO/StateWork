# Workstation setup

## Units

In the installed CAD app, set the default length units to millimetres. Save the local preference.

## Check

Create a temporary new document and inspect its units indicator. Setup is complete when that new document uses millimetres. Close the temporary document without saving it. This is local preparation; there is no setup submission or approval.
